alerts display when user typed something wrong in registration form & added successfully registered alert to the login page & turned off thymeleaf caching & fixed register anchoir in login template





(			# Start of group
  (?=.*\d)		#   must contains one digit from 0-9
  (?=.*[a-z])		#   must contains one lowercase characters
  (?=.*[A-Z])		#   must contains one uppercase characters
  (?=.*[@$!%*#?&])		#   must contains one special symbols in the list "@#$%"
              .		#     match anything with previous condition checking
                {6,}	#        length at least 6 characters and maximum of infinity
)			# End of group



^     # Match the start of the string
\\s*  # Match zero of more whitespace characters
$     # Match the end of the string